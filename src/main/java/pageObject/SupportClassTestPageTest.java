package pageObject;

public class SupportClassTestPageTest {


    /*
    test using abstraction is
    -easy to read
    -only has to change if intent of test changes
    -doesnt have to change if application changes-the page objects change
    -page objects used in multiple tests

     */
}
